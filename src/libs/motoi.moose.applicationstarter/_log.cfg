# Common properties
level = DEBUG
pattern = %level %date(dd.MM.yyyy HH:mm:ss) [%thread] %name - %text%nl
streams = cs

# Console stream properties
cs = ConsoleStream

# File stream properties
fs = FileStream
fs.fileName = _motoi-log.txt
fs.Append = true